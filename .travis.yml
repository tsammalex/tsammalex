language: python
python:
  - "3.4"
install:
  - pip install -U setuptools
  - pip install nose coverage mock
script:
  - python setup.py install
  - nosetests
notifications:
  email:
    - forkel@shh.mpg.de
